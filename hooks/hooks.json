{
  "description": "Refresh DEX usage before startup status rendering and warm it asynchronously on prompts",
  "hooks": {
    "SessionStart": [{
      "matcher": "startup|resume|clear|compact",
      "hooks": [{
        "type": "command",
        "command": "python3",
        "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/dex_usage.py", "hook-startup"],
        "timeout": 8
      }]
    }],
    "UserPromptSubmit": [{
      "hooks": [{
        "type": "command",
        "command": "python3",
        "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/dex_usage.py", "hook-warm"],
        "async": true,
        "timeout": 30
      }]
    }]
  }
}
